spring:




  datasource:
    url: jdbc:mariadb://localhost:13306/crepenboard
    username: crepen-board
    password: qwer1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # ?? ?: create / update / validate / none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  output:
    ansi:
      enabled: ALWAYS